objectives:

Clearly define scope.
Understand what we have already completed.
Make strong foundation.

control statements:
if, for, while

limitless number of variables:

operands:
==, >=. <=, <, >, =

datatypes:
number, boolean, string

input:
virtual keyboard, with arrows and 4 function keys.

output:
screen with 3 sprites max.
uiupdate() function to update


UI blocks:
- number, string, bool.
- all operands.
if: contains 1 variable, one operand, 1 variable or fixed value.
for: fixed-value step number, variable or fixed value.
while: condition, operand, condition

sprites:
3 max, each have x and y pos. updated by user reasigning them.

UI panels = pallete left whole screen, code window right bottm, output window right top.
